import React from "react";
import PropTypes from "prop-types";

interface indexProps {}

const index = ({}: indexProps) => {
  return <div>This is index Component/Page</div>;
};

index.propTypes = {};

export default index;
